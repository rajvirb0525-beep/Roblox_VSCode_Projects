local Types = {}
-- // This keeps types apart from code to avoid huge scripts and 
export type R6Rig = {
	Humanoid: Humanoid,
	Head: BasePart,
	HumanoidRootPart: BasePart,
	Left_Arm: BasePart,
	Left_Leg: BasePart,
	Right_Arm: BasePart,
	Right_Leg: BasePart,
	Torso: BasePart
}

return Types
